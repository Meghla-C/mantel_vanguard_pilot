computer_share:
  enabled: true
  source:
    reader_type: file_stream
    format: parquet
    volume_path: 'vanguard_landing/computershare-dummy-data/*.parquet'
    read_options: {"cloudFiles.includeExistingFiles": True,
                  "cloudFiles.schemaEvolutionMode": "none",
                  "cloudFiles.allowOverwrites": True,
                  "header": False}
  target:
    table_name: vanguard_bronze.computershare
    writer_type: append_stream
    trigger_type_value: {"availableNow": True}
